include "common.conf"

kafka {
  bootstrap.servers = ${KAFKA_HOSTS_LEGACY}
  aiven {
    hosts = ${KAFKA_HOSTS_AIVEN}
    schema_registry {
      url = ${SCHEMA_REGISTRY_URL}
      username = ""
      password = ""
    }
  }
  ssl.enabled = false
}